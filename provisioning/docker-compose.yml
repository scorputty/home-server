# This file will be called from Ansible

version: '2'

services:
  # couchpotato:
  #   container_name: couchpotato
  #   build: ./couchpotato
  #   image: alpine:latest
  #   ports:
  #     - "5050:5050"
  #   volumes:
  #     - /shared/config/couchpotato:/config/couchpotato
  #     - /shared/data:/data
  #     - /shared/data/cache:/cache
  sabnzbd:
    container_name: sabnzbd
    build: ./sabnzbd
    image: alpine:latest
    ports:
      - "8080:8080"
    environment:
      - UID=vagrant
      - GID=vagrant
    volumes:
      - /shared/config/sabnzbd:/config/sabnzbd
      - /shared/data:/data
    restart: always
